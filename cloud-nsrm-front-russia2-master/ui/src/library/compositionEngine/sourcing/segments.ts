// import { generateCharFunctionExpression } from '@meicloud/render-engine'

/**
 * @description 物料编码判断显示 无编码物料 - 显示为空
 * @author donghf3
 * @param targetNum 物料编码
 * @returns {string|*}
 */
export const itemCodeRevealSegment = {
  // 'x-content': generateCharFunctionExpression(({ $self }) => {
  //   return $self.value + '1'
  // })
  'x-content': 'adsfasdf'
  // return targetNum && targetNum.indexOf('VIRTUAL_') === 0 ? '' : targetNum
}
